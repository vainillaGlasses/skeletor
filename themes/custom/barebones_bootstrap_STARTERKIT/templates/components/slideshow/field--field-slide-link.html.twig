{#
/**
 * @file
 * Hero title field override.
 *
 */
#}

{%
  set classes = [
    'field',
    'field--name-' ~ field_name|clean_class,
    'field--type-' ~ field_type|clean_class,
    'field--label-' ~ label_display,
    'buttons',
  ]
%}
{% for item in items %}
  {{ item.content|merge({'#options': {'attributes': {'class': ['slideshow_botton']}}}) }}
{% endfor %}